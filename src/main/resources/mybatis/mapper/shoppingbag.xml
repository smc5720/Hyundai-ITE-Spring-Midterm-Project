<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mycompany.webapp.dao.ShoppingbagDao">
	<insert id="insertProduct" parameterType="shoppingbag">
		INSERT INTO shoppingbag(SBNO, SBPRODUCTCOLOR, SBPRODUCTSIZE, SBPRODUCTAMOUNT, MNO, PCODE)
    	VALUES(sbno_seq.nextval, #{sbproductcolor}, #{sbproductsize}, #{sbproductamount}, #{mno}, #{pcode})
	</insert>
	<select id="selectShoppingProducts" parameterType="int" resultType="shoppingbag">
		SELECT SBNO, SBPRODUCTCOLOR, SBPRODUCTSIZE, SBPRODUCTAMOUNT, MNO, PCODE
		FROM shoppingbag
		WHERE mno = #{mno}
	</select>
</mapper>
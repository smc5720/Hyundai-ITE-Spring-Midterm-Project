<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mycompany.webapp.dao.ProductDao">
	<select id="selectProducts" resultType="product" parameterType="category">
		SELECT p.pcode, p.pname, p.pprice, p.pdetail,
			p.precommanditem, b.bname, p.clarge, p.cmedium, p.csmall
		FROM product p, brand b
		WHERE p.clarge = #{cLarge}
		AND p.cmedium = #{cMedium}
		AND p.csmall = #{cSmall}
		AND b.bno = p.bno
	</select>
	<select id="selectProductColor" resultType="productcolor" parameterType="product">
		SELECT pcode, cproductcolor, cimageproduct1, cimageproduct2, cimageproduct3, ccolorchipimage
		FROM color
		WHERE pcode = #{pcode}
	</select>
</mapper>